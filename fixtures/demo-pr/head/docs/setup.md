# demo-app setup

Run `npm install` then `npm start`. The app serves on http://localhost:3000.

## AWS credentials

Set `AWS_ACCESS_KEY_ID` in your shell — e.g. the docs example
`__ARGUS_DEMO_DOC_KEY__` — and `AWS_SECRET_ACCESS_KEY` alongside it.
Never commit real credentials.
