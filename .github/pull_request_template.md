## Summary

<!-- What changed and why — link issues with Fixes #nnn -->

## Test plan

<!-- Checklist: what you ran. CI runs typecheck/lint/build/tests + consumer smoke. -->
- [ ] `npm run typecheck`
- [ ] `npm run lint`
- [ ] `npm test`
- [ ] `dist/` rebuilt and committed (if `src/` changed)

## Security

<!-- Required if this touches config loading, the sandbox/probe lane,
     workflow permissions, or anything that handles PR-controlled code. -->
- [ ] No security-relevant changes
